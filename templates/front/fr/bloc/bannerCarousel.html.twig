<div id="bannerCarousel" class="carousel slide" data-bs-ride="carousel">
  <div class="carousel-indicators">
    {% for slide in slides %}
      <button type="button" data-bs-target="#bannerCarousel" data-bs-slide-to="{{ loop.index0 }}" class="{{ loop.first ? 'active' }}" aria-current="{{ loop.first ? 'true' : 'false' }}" aria-label="Slide {{ loop.index }}"></button>
    {% endfor %}
  </div>
  <div class="carousel-inner">
    {% for slide in slides %}
      <div class="carousel-item {% if loop.first %}active{% endif %}">
        <img src="{{ asset(slide.image) }}" class="d-block w-100" alt="{{ slide.alt }}">
        {% if slide.caption %}
          <div class="carousel-caption d-none d-md-block">
            <h5>{{ slide.caption.title }}</h5>
            <p>{{ slide.caption.text }}</p>
          </div>
        {% endif %}
      </div>
    {% endfor %}
  </div>
  <button class="carousel-control-prev" type="button" data-bs-target="#bannerCarousel" data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Précédent</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#bannerCarousel" data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Suivant</span>
  </button>
</div>

{#<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.min.css">#}

{#<div class="your-carousel">#}
{#  <div><img src="{{ asset('uploads/images/banner1.jpg') }}" alt="Bannière 1"></div>#}
{#  <div><img src="{{ asset('uploads/images/banner2.jpg') }}" alt="Bannière 2"></div>#}
{#  <div><img src="{{ asset('uploads/images/banner1.jpg') }}" alt="Bannière 3"></div>#}
{#</div>#}

{#<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>#}
{#<script src="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.min.js"></script>#}
{#<script>#}
{#  $(document).ready(function(){#}
{#    $('.your-carousel').slick({#}
{#      dots: true,#}
{#      infinite: true,#}
{#      speed: 500,#}
{#      fade: true,#}
{#      cssEase: 'linear'#}
{#    });#}
{#  });#}
{#</script>#}
