{{ form_start(form) }}

<div class="my-custom-class-for-errors">
    {{ form_errors(form) }}
</div>

<div class="row">
    <div class="col">
        {{ form_row(form.title) }}
    </div>
</div>
<div class="row">
    <div class="col">
        {{ form_row(form.scope) }}
    </div>
</div>
<div class="row">
    <div class="col" id="some-custom-id">
        {{ form_row(form.intro) }}
        {#            {{ form_widget(form.scope, {'value' : content.scope }) }} #}
    </div>
    <small>{{ form_help(form.body) }}</small>
{% if app.session.get('current_scope').id == 15 %}
<div>
    <a class="btn btn-sm btn-default" href="#" id="load_gallery" >Load Galleries</a>
    </a>
    <input type="hidden" id="content_scope" value="{{ app.session.get('current_scope').id }}"  />
    <input type="hidden" id="content_id" value="{{ content.id }}"  />
</div>
{% endif %}
    <div class="form-error">
        {{ form_errors(form.body) }}
    </div>
</div>

<div class="row">
    <div class="col" id="some-custom-id">
        {{ form_row(form.body) }}
    </div>
    <small>{{ form_help(form.body) }}</small>

    <div class="form-error">
        {{ form_errors(form.body) }}
    </div>
</div>


<div class="row">
    <div class="col" id="some-custom-id">
        {{ form_row(form.tags) }}
    </div>
    <small>{{ form_help(form.tags) }}</small>

    <div class="form-error">
        {{ form_errors(form.tags) }}
    </div>
</div>
<div class="row">
    <br/><br/><br/><br/>
    <div class="col" id="some-custom-id">
        {{ form_widget(form.picture) }}
    </div>
    <small>{{ form_help(form.picture) }}</small>

    <div class="form-error">
        {{ form_errors(form.picture) }}
    </div>
    <div class="col-lg-6">
        <div class=""><img src="{{ asset('uploads/'~ content.picture) }}" alt="{{ content.picture }}"/></div>
    </div>
</div>

<div class="row">
    <div class="col">
        <div class="" id="some-custom-id">
            {{ form_row(form.published_date) }}
        </div>
        <small>{{ form_help(form.published_date) }}</small>
        <div class="form-error">
            {{ form_errors(form.published_date) }}
        </div>
    </div>
    <div class="col form-switch">
        <label class="col-lg-3 control-label pl-1 pt-2 col-lg-3">Published</label>
        {{ form_label(form.published, 'Published') }}
        {{ form_row(form.published) }}
    </div>
    <br/>
</div>

<div class="row">
    <br/>
    <div class="col" id="some-custom-id">
        <label for="content_published_date" class="form-label required">Article sur toute la largeur  </label>
        {{ form_row(form.fullWidth) }}
    </div>
    <small>{{ form_help(form.fullWidth) }}</small>

    <div class="form-error">
        {{ form_errors(form.fullWidth) }}
    </div>
</div>
<div class="mt-3">
    <button class="btn btn-sm btn-primary">{{ button_label|default('Save') }}</button>
</div>
{#<style>#}
{#    #cke_1_contents {#}
{#        height: 1000px !important#}
{#    }#}

{#    .ck-editor__editable_inline {#}
{#        min-height: 800px !important;#}
{#    }#}

{#</style>#}

{{ form_end(form) }}
