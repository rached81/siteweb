<form method="post" action="{{ path('app_time_line_delete', {'id': time_line.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ time_line.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
